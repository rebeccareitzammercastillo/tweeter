<p id="notice"><%= notice %></p>

<div class="section">
    <div class="container">
        <div class="columns is-centered">
            <div class="column is-half">
                <p class="title is-4">Chirp: <%= @chirp.chirp %></p>
                <%= link_to 'Edit', edit_chirp_path(@chirp) %> |
                <%= link_to 'Back', chirps_path %>
            </div>
        </div>
    </div>
</div>
